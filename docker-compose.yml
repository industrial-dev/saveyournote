services:
  api:
    build:
      context: .
      dockerfile: src/SaveYourNote.Api/Dockerfile
    ports:
      - "5001:5001"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - WhatsApp__VerifyToken=dev_verify_token_12345
    volumes:
      - .:/app
    restart: always
